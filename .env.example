# fal.ai – SAM 3D Body mesh + keypoints (required for photo-based GLB; without FAL_KEY we use a placeholder body)
FAL_KEY=

# AI Sizing / body measurements API (optional; without it we use height-scaled placeholder)
# AI_SIZING_API_KEY=

# Per-merchant API key for embed / headless (optional). When set, cross-origin API calls must send Authorization: Key <key>
# TRYON_API_KEY=
